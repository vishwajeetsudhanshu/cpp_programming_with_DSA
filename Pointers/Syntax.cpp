#include<iostream>
using namespace std;
int main(){
    int x =10,y=5;
    int* p1=&x;
    int* p2=&y;
    // int*p1=&x,p2=&y; it givees an error
    //Always use to declare pointers in seprate lines otherwise it will throw an error
    
    
}